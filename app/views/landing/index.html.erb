<!DOCTYPE html>
<html>
<head>
  <title>Viewing Party</title>
  <style>
    body {
      background-color: #CFEAE4;
      color: #444;
      font-family: Arial, sans-serif;
      text-align: center;
      padding-top: 50px;
    }
    h1 {
      font-size: 4em;
      margin-bottom: 20px;
    }
   
  </style>
</head>
<body>
  <h1>Welcome to Viewing Party</h1>
  
  <% if current_user %>
    <div id="logout">
      <%= button_to "Log Out", logout_path, method: :get, :class => "discover" %>
    </div>
  <% else %>
    <%= button_to "Register as a User", register_path, method: :get, :class => "discover" %> 
    <br>
    <%= button_to "I already have an account", login_path, method: :get, :class => "discover" %>
  <% end %>
</body>
</html>
